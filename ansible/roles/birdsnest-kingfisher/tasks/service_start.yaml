---
- name: Reload systemd to recognize new kingfisher unit
  ansible.builtin.systemd:
    daemon_reload: yes
  when: ansible_os_family in ["RedHat", "Debian", "Alpine"]
  
- name: Start kingfisher Service (Linux/Unix)
  service:
    name: "kingfisher-{{ kingfisher_agent_name if ansible_os_family != 'FreeBSD' else '_' + kingfisher_agent_name }}"
    state: started
    enabled: yes
  when: ansible_os_family in ["RedHat", "Debian", "Alpine", "FreeBSD"]

- name: Start kingfisher Service (Windows)
  win_service:
    name: "kingfisher-{{ kingfisher_agent_name }}"
    start_mode: auto
    state: started
  when: ansible_os_family == "Windows"